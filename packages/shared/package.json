{
  "name": "crew-shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared types, schemas, and loaders consumed by crew's CLI, daemon, and dashboard.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "smol-toml": "^1.6.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  }
}
